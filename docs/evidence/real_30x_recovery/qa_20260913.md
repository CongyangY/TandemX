# Comparator and recovery QA — 2026-09-13

All checks ran in the project `tandemx-dev` conda environment. They did not
write to T7 or launch a large-data comparator.

The focused suite covering the historical prioritization endpoint, YSD56
TandemX--TideHunter postprocessor, T7 recovery audit and formal real-30x runner
passed 32 tests. After independent code review and the resulting boundary-case
fixes, the complete repository suite passed 864 tests in 146.34 seconds:

```text
conda run -n tandemx-dev pytest -q
864 passed in 146.34s (0:02:26)
```

The final fixes keep unavailable comparator counts as `N/A`, reject unknown
historical reference states and non-positive read estimates, restrict recovery
audits to explicitly named blocks with bounded traversal and streaming hashes,
and refuse the 3.53-billion-pair YSD56 permissive screen without interpreting
the 9,971 unaligned families as unsupported or TandemX-only.
