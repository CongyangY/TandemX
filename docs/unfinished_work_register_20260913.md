# TandemX unfinished-work register — 2026-09-13

This register records the current execution boundary after the T7
unmount/remount event. It is an operational handoff, not a manuscript claim.
Zero-byte, missing or interrupted files are not counted as completed evidence.

## Completed

- Manuscript v5 local text, Word build, rendered QA package, five main Results,
  figure/table map, and main/supplementary figure assets.
- Discovery-saturation benchmark: seven depths × three seeds, 21/21 runs;
  operational all-seed saturation rule and truth-recovery conclusion recorded.
- Formal SRF unified simulation comparator: 72/72 cells, including retained
  N/A/no-catalogue outcomes and negative-background accounting.
- A3 gate decision and exact rolling-code implementation optimization. No new
  production algorithm was accepted.
- TXF000708 bounded YSD56 evidence package, including the user's BLAST result;
  state remains a previously-unreported-in-source-article candidate, not a
  globally established novel TR.
- YSD56 recurrent/exact TideHunter reconstruction from an internally recovered,
  SHA-verified normalization database: 12,070 recurrent TandemX families,
  2,099 exact TideHunter-supported and 9,971 nonexact technical unresolved.
  The 3,529,718,697-pair exhaustive permissive search was refused by the frozen
  five-million-pair resource gate; no unsupported or TandemX-only count was made.

## Ready to replay or finish after a safe storage route is available

- Six T2T peanut materials plus soybean ZH13/related 30x cohort inventory and
  cross-tool runs. First rebuild only from verified source metadata and
  durable checksums, with direct download preferred and proxy use budgeted.
  Required inputs: donor/read correspondence, accession endpoints/checksums,
  assembly spans, storage headroom and a verified T7 or approved staging path.
- Final real-data comparator endpoint integration after native outputs are
  durable: family recovery, abundance error, false attribution,
  divergent/indel robustness, runtime and peak RSS; mark unsupported endpoints
  N/A. Required inputs: common assembly/read binding and all tool receipts.
- Final old/new rolling-code scaling benchmark after the performance table is
  frozen: approximately 100 Mb and 1 Gb where resources permit, three isolated
  repeats per version, wall/CPU/RSS/throughput/disk/output identity.

## Blocked

- T7 read-only filesystem verification: current mount is held by unrelated
  processes, so `verifyVolume`/ExFAT consistency checks cannot safely run.
  Required input/action: user or host-level release of those processes, or an
  explicitly approved safe recovery route.
- K30076 real 30x blocks: block receipts/plans are missing or zero-byte after
  remount; pre-remount observations are not durable. Required inputs: verified
  source files/receipts and a clean rebuild location.
- V14167 formal three-tool comparison: partial/interrupted TRF files remain,
  but the complete nonzero receipt set is not durable. Required inputs: source
  FASTQ/SRA route, partition contract and verified storage.
- Historical-assembly SRF/ordinary-mapping prioritization: local code exists,
  but native comparator evidence required for the endpoint is unavailable.
  Required inputs: durable native historical outputs plus frozen historical
  assembly/read mappings.
- YSD56 nonexact TideHunter support: 9,971 recurrent exact-unmatched families
  imply 3.53 billion length-eligible pairs under the frozen rule. A sampled
  lossless q-gram necessary condition retained 77.2% and did not make exhaustive
  alignment feasible. Required input/action: a separately governed,
  comparator-only lossless candidate index with no-false-negative validation;
  until then every nonexact family remains technical unresolved.

## Deferred by user or explicit scope

- Bioconda, Zenodo and formal tagged release: paused until the user
  reauthorizes after comparator and manuscript structure freeze.
- Sixth manuscript Result: reserved for the user's own future TR validation.
- BLAST execution/submission follow-up: user will perform it; no sequence is
  submitted to GitHub by this task.
- Brachypodium analysis: withdrawn because the applicable public input is a
  very large BAM and does not meet the current operational/T2T scope.
- New abundance/detector/context algorithms, A3 third round, HMM/EM,
  multi-representative redesign, deep learning, and sequence repair: closed by
  the current scientific gate.
- Approximately 1-Gb scaling before the manuscript performance table is
  frozen: intentionally deferred to avoid an unbounded benchmark.

## Execution order when unblocked

1. Verify T7 safely and inventory nonzero source/evidence files.
2. Rebuild only invalidated K30076/V14167 cells and obtain durable receipts.
3. Finish historical prioritization using the frozen protocol; retain the
   bounded YSD56 permissive state unless a separately validated lossless index
   becomes available.
4. Run the bounded peanut/soybean cohort and integrate only supported common
   endpoints.
5. Freeze the manuscript performance table, run final scaling, then perform
   final manuscript consistency and release-gate review. Release publication
   remains a separate later authorization.
