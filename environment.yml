name: tandemx-dev
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pytest
  - pyyaml
  - matplotlib
  - rust
  - maturin
  # Small independent edit-distance evaluator; not used by TandemX discovery.
  - pip:
      - edlib==1.3.9.post1
