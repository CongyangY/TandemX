# Quantify FASTA-survival replay

This compact archive verifies that the committed all-ACGT FASTA shortcut preserved the frozen development matrix exactly: all scientific metric rows, per-execution copy-number products, control panels and non-resource summary fields agree. Resource values are one historical run and one same-machine replay, so they support an engineering regression check rather than a publication timing distribution.
