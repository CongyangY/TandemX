# Supplementary Figure S8 — Ey15 donor-matched reference context

Rendered locally by `../build_s7_s8_s10.py` from the frozen sources listed in `panel_source.tsv`. PNG was visually reviewed; PDF is one page; SVG uses editable vector marks and text.
