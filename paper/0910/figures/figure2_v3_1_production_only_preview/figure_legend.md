**Figure 2 | Controlled read-based validation.** (A) Family recovery, interval F1 and base-union F1 for TandemX, TRF and TideHunter across 13 positive held-out scenarios, each run with three seeds. (B) Discovery recovery and positive-family mean absolute relative error (MARE) across six formal conditions with three seeds per condition. Competitive mapping is shown for MARE but has no independent discovery endpoint because it uses the TandemX catalogue. (C) Mean bases attributed to non-target reads in the shared-fragment condition across three formal replicates. All panels are direct redraws of the frozen TSV evidence listed in `panel_source.tsv`.
