# Figure 1 AI-generated biology-first schematic

Status: generated raster draft for author refinement. This supersedes the
rejected editable drafts in `figure1_tandemx_overview/` and
`figure1_v2_biology_first/`, but it is not labelled final until the author
accepts it.

The image was generated with the built-in OpenAI image-generation tool. It has
no embedded text so that panel letters and exact terminology can be added by
the author during final layout.

## Generation prompt

Create a polished biology-first scientific illustration showing that raw HiFi
reads can support a longer satellite DNA array than is represented in an
otherwise continuous chromosome-scale assembly, and that TandemX links the
same repeat family from raw-read discovery to a family-resolved
read-versus-assembly audit. Use a wide three-zone composition: read-supported
long array versus a shorter array in continuous assembly; periodic intervals,
repeat-family consensus, diagnostic k-mer counting and assembly localization;
paired read/assembly abundance bars with supported and unresolved evidence
states. Use a clean journal schematic style on white, with navy/blue reads,
coral repeat units, slate assembly flanks, green support and amber unresolved.
No text, letters, numbers, title, legend, logo or watermark.

## Provenance

- Built-in image-generation output retained at its default generated-image
  location.
- Project copy: `figure1_ai_biology_first_v1.png`.
- The figure is conceptual. It contains no empirical numerical values and must
  not be interpreted as a measured array reconstruction.
